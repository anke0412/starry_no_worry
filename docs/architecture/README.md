# Architecture Docs Index

Current stable architecture references:

- `plan.md` for frontend/backend boundary and recommended module layout
- `docs/api-contracts.md` for frontend/backend chart contracts
- `docs/project-memory-zh.md` for durable generation-framework direction

Add new long-lived architecture decisions here when they are no longer just runtime execution notes.
