# Frontend Boundaries

Frontend owns:

- chart mode and chart type selection
- profile input flows and settings exposure
- local persistence
- wheel, tables, and result interaction
- request assembly and response rendering

Frontend must not own:

- ephemeris math
- silent reinterpretation of backend chart data
