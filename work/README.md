# Work Directory

`work/` stores runtime execution artifacts for the harness.

## Subdirectories

- `backlog/` for candidate tasks
- `plans/` for active and completed execution-ready plans
- `runs/` for run checkpoints
- `decisions/` for temporary decision capture
