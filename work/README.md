# Work Directory

`work/` stores runtime execution artifacts for the current harness.

## Subdirectories

- `backlog/` for not-yet-approved ideas
- `plans/` for execution-ready task plans
- `runs/` for session checkpoints and run logs
- `decisions/` for temporary decision capture before promotion
